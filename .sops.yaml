# Make a user key
# mkdir -p ~/.config/sops/age
# age-keygen -o ~/.config/sops/age/keys.txt
# chmod 600 ~/.config/sops/age/keys.txt

# Display the user public key:
# age-keygen -y ~/.config/sops/age/keys.txt

# Make a host public key:
# nix-shell -p ssh-to-age
# ssh-to-age -i /etc/ssh/ssh_host_ed25519_key.pub

# Add secrets
# sops secrets/secrets.yaml

# Rekey secrets.yaml after editing recipients in .sops.yaml:
# sops updatekeys secrets/secrets.yaml
keys:
  - &users:
    - &alexander 07E36E87C0270E464DB4D663B423D1630A58837C
    
  - &hosts:
    - &desktop age1kzxen8ynw28h935jnjc9t8cx4p8mv6angk5suvkp6ntu4am9pe4smfw22z
    - &vm age1fy626vylmpya4t4gyqx6kw4ggn7j79upjgt4jka2gq6lysluhd0s8ytqls
    
creation_rules:
  - path_regex: modules/nixos/roles/kubernetes/secrets.ya?ml$
    key_groups:
    - age:
      - *desktop
      - *vm
   
  - path_regex: modules/nixos/secrets.ya?ml$
    key_groups:
    - age:
      - *desktop
      - *vm
    - gpg:
      - *alexander
      

  - path_regex: modules/nixos/services/secrets.ya?ml$
    key_groups:
    - age:
      - *desktop
      - *vm
    - gpg:
      - *alexander

  - path_regex: modules/home/secrets.ya?ml$
    key_groups:
    - age:
      - *desktop
      - *vm
    - gpg:
      - *alexander
