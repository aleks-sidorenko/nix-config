_:
{
  # NOTE: LCD, serial port, SMB, and wireless security profile settings
  # are not managed via Terraform as they lack dedicated provider resources.
  # These settings are left at their defaults on the router.
}
